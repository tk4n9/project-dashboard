{
  "name": "project-dashboard",
  "version": "0.1.0",
  "description": "Notion-like project status dashboard with per-todo Claude Code remote-control sessions",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "init-db": "node src/db.js --init",
    "test": "node --test",
    "check": "node scripts/check.mjs",
    "new:page": "node scripts/new-page.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  }
}
